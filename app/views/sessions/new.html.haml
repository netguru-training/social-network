.jumbotron#signin
  = link_to 'Sign in with Facebook', 'auth/facebook', class: 'btn btn-primary', id: 'signin'

if current_user
  %a{ href: 'signout'} Sign out
  %img{ src: current_user.image }
  %b= current_user.location
else
  %a{ href: 'auth/facebook' } Sign in via Facebook
